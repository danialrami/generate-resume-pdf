# Changelog

## [Unreleased]
- Added documentation for the new YAML workflow in README.

## [1.0.1] - 2025-10-23
- Added YAML‑driven resume workflow using `yaml-resume-builder`.
- Created `resume.yaml` with sample data and updated template in `templates/resume.tex`.
- Updated README to explain new workflow, usage, and template customization.
- Added `requirements.txt` with `yaml-resume-builder==1.1.2`.
- Updated build commands to use the new CLI and one‑page optimization.
